const EventEmitter = require('events');
const emitter = new EventEmitter();

emitter.on('saved', (data) => {
    // console.log("A saved event occured.");
    console.log(data);
});

emitter.emit('saved', {message: "Triggered"});